# Internal Deepiri libraries (pip): track main (tip of branch). For reproducible installs,
# prefer Poetry + committed poetry.lock; pip from this file follows branch HEAD.
# Requires GitHub auth for private repos (e.g. git credential or pip config).
deepiri-dataset-processor[all] @ git+https://github.com/Team-Deepiri/deepiri-dataset-processor.git@main
deepiri-training-orchestrator @ git+https://github.com/Team-Deepiri/deepiri-training-orchestrator.git@main
deepiri-gpu-utils[torch] @ git+https://github.com/Team-Deepiri/deepiri-gpu-utils.git@main

# ML Training Dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
datasets>=2.14.0
mlflow>=2.8.0
optuna>=3.4.0
wandb>=0.15.0
jupyter>=1.0.0
notebook>=7.0.0

# Data processing
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# Tokenization
sentencepiece>=0.1.99

# Model components
einops>=0.7.0

# Model evaluation
evaluate>=0.4.0
rouge-score>=0.1.2

# Attention optimizations (optional)
# flash-attn>=2.0.0  # Install separately: pip install flash-attn --no-build-isolation

# Model export
onnx>=1.14.0
onnxruntime>=1.15.0

# Event streaming (Synapse integration)
redis>=5.0.0
hiredis>=2.2.0

# Embeddings for semantic deduplication
sentence-transformers>=2.2.0  # Optional, for semantic deduplication

# Privacy & security (optional)
# presidio-analyzer>=2.2.0  # For PII detection
# presidio-anonymizer>=2.2.0

# Shared dependencies
# deepiri-modelkit: Installed from /workspace/deepiri-modelkit in Dockerfile
# For local development: pip install -e ../deepiri-modelkit

# Dataset Versioning Dependencies
dvc>=3.0.0
dvc-s3>=3.0.0
pydantic>=2.0.0
sqlalchemy>=2.0.0
alembic>=1.12.0

# CLI Interface
click>=8.1.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0
python-dotenv>=1.0.0

# GPU / device policy (shared with Cyrex via deepiri-gpu-utils). Poetry pins git revs in pyproject.toml.
# pip-only (match pyproject revs when bumping):
deepiri-gpu-utils @ git+https://github.com/Team-Deepiri/deepiri-gpu-utils.git@98dc2f239ac585301024bcb538aa92c994034465
deepiri-dataset-processor[all] @ git+https://github.com/Team-Deepiri/deepiri-dataset-processor.git@main
