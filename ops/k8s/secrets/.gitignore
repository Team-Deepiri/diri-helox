# Kubernetes Secrets - NEVER commit ANYTHING
# This directory is for k8s secret YAML files
# ALL files are gitignored - developers must create their own locally

# Ignore everything in this directory
*

# Only allow this gitignore file itself
!.gitignore
