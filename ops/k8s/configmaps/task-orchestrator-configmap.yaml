apiVersion: v1
kind: ConfigMap
metadata:
  name: task-orchestrator-config
  namespace: default
  annotations:
    description: "ConfigMap for Task Orchestrator Service - public configuration"
data:
  # Server Configuration
  NODE_ENV: "development"
  PORT: "5002"
  
  # Database URLs (using Docker service names)
  DATABASE_URL: "postgresql://deepiri:deepiripassword@postgres:5432/deepiri"
  
  # Streaming Service Configuration (for publishing task events)
  SYNAPSE_URL: "http://synapse:8002"

