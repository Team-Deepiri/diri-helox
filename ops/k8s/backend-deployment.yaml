apiVersion: apps/v1
kind: Deployment
metadata:
  name: trailblip-backend-blue
spec:
  replicas: 2
  selector:
    matchLabels:
      app: trailblip-backend
      color: blue
  template:
    metadata:
      labels:
        app: trailblip-backend
        color: blue
    spec:
      containers:
        - name: backend
          image: yourrepo/trailblip-backend:latest
          ports:
            - containerPort: 5000
          env:
            - name: NODE_ENV
              value: production
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: trailblip-secrets
                  key: jwt_secret
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: trailblip-secrets
                  key: openai_api_key
            - name: MONGODB_URI
              valueFrom:
                secretKeyRef:
                  name: trailblip-secrets
                  key: mongo_uri
          resources:
            requests:
              cpu: "200m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: trailblip-backend-hpa
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: trailblip-backend-blue
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60

