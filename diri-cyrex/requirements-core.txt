# Core dependencies (torch is already in base PyTorch image)
fastapi==0.112.2
uvicorn[standard]==0.30.6
pydantic==2.8.2
openai==1.43.0
python-dotenv==1.0.1
httpx==0.27.2
pytest==8.3.2
pytest-asyncio==0.23.5
pytest-cov==4.1.0
structlog==24.1.0
python-json-logger==2.0.7
prometheus-client==0.20.0
redis==5.0.1
pydantic-settings==2.2.1

# AI Training Dependencies (torch excluded - already in base image)
transformers>=4.30.0
datasets>=2.14.0
accelerate>=0.20.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0

# Optional heavy packages (can fail without breaking build)
# deepspeed>=0.12.0  # For distributed training (very large, install separately if needed)
# mlflow>=2.7.0  # Install separately if needed
# wandb>=0.15.0  # Install separately if needed

# New AI Service Dependencies
peft>=0.7.0  # For LoRA adapters
# bitsandbytes>=0.41.0  # For quantization (GPU only, install separately if needed)
gymnasium>=0.29.0  # For RL environment

# Vector Database Dependencies
pymilvus>=2.3.0  # For Milvus vector database
pinecone-client>=3.0.0  # For Pinecone (optional)
weaviate-client>=4.0.0  # For Weaviate (optional)

# Time-Series Analytics
influxdb-client>=1.38.0  # For InfluxDB

# MLOps Dependencies
kubernetes>=28.1.0  # For Kubernetes deployment

# Additional ML Libraries (optional)
optuna>=3.5.0  # For hyperparameter optimization
hyperopt>=0.2.7  # For hyperparameter optimization
tensorboard>=2.15.0  # For visualization

