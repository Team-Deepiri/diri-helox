# Docker BuildKit Configuration
# This ensures builds are efficient and don't leave dangling images

[worker.oci]
  # Automatically remove intermediate containers
  gc = true
  # Keep only the final image
  gckeepstorage = 10000  # 10GB max for build cache
  
[worker.containerd]
  gc = true
  gckeepstorage = 10000

