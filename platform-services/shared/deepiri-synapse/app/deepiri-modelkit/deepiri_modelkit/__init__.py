"""deepiri_modelkit package (minimal init for local copy).

This file makes the local `deepiri_modelkit` directory a Python package so
`deepiri_modelkit.contracts` imports work when the parent directory is on
`PYTHONPATH`.
"""

__all__ = ["contracts"]
